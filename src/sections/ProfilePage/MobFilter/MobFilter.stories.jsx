import React from 'react'
import Filter from './index'

export default {
	title: 'Sections/MobFilter',
	component: FilterView,
}

const Template = () => {
	return <Filter />
}

export const FilterView = Template.bind({})
