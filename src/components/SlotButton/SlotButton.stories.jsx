import React from 'react'
import SlotsButton from './index'

export default {
	title: 'components/SlotButton',
	component: Button,
}

const Template = () => {
	return <SlotsButton />
}

export const Button = Template.bind({})
