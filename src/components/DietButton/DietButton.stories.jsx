import React from 'react'
import Button from './index'
export default {
	title: 'components/DietButton',
	component: DietButton,
}

const Template = () => {
	return <Button />
}

export const DietButton = Template.bind({})
