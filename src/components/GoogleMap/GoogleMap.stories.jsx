import React from 'react'
import BookingMap from './index'

export default {
	title: 'components/BookingMap',
	component: BookingMap,
}

const Template = () => {
	return <BookingMap />
}

export const DoctorBookingMap = Template.bind({})
