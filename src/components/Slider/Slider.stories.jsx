import React from 'react'
import Slider from './index'

export default {
	title: 'components/Slider',
	component: Slider,
}

const Template = () => {
	return <Slider />
}

export const LandingSlider = Template.bind({})
