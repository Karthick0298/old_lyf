import React from 'react'
import Card from './SpaCard'

export default {
	title: 'components/Card',
	component: Card,
}

const Template = () => {
	return <Card />
}

export const SpaCard = Template.bind({})
