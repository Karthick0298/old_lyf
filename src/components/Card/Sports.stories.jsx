import React from 'react'
import Card from './SportsCard'

export default {
	title: 'components/Card',
	component: Card,
}

const Template = () => {
	return <Card />
}

export const SportsCard = Template.bind({})
