import React from 'react'

import Slider from './index'

export default {
	title: 'Components/Slider',
	component: Slider,
}

const Template = () => {
	return <Slider />
}

export const LandingSliderTwo = Template.bind({})
