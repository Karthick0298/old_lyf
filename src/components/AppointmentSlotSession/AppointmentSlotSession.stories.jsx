import React from 'react'
import AppointmentSlot from './index'

export default {
	title: 'components/AppointmentSession',
	component: AppointmentSession,
}

const Template = () => {
	return <AppointmentSlot />
}

export const AppointmentSession = Template.bind({})
