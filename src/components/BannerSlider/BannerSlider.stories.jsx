import React from 'react'
import BgSlider from './index'

export default {
	title: 'components/BackgroundSlider',
	component: BackgroundSlider,
}

const Template = () => {
	return <BgSlider />
}

export const BackgroundSlider = Template.bind({})
