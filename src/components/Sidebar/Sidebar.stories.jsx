import React from 'react'
import Sidebar from './index'

export default {
	title: 'components/Sidebar',
	component: Sidebar,
}

const Template = () => {
	return <Sidebar />
}

export const LayoutSidebar = Template.bind({})
