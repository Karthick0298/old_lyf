import React from 'react'
import NumberGrid from './index'

export default {
	title: 'components/NumberGridList',
	component: NumerGrid,
}

const Template = () => {
	return <NumberGrid />
}

export const NumerGrid = Template.bind({})
