import React from 'react'
import NumeroListGrid from './index'

export default {
	title: 'components/NumeroListGrid',
	component: NumerList,
}

const Template = () => {
	return <NumeroListGrid />
}

export const NumerList = Template.bind({})
