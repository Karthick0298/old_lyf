import React from 'react'
import Diet from './index'
export default {
	title: 'components/DietCard',
	component: DietCard,
}

const Template = () => {
	return <Diet />
}

export const DietCard = Template.bind({})
