import React from 'react'
import AppointmentSlotPicker from './index'

export default {
	title: 'components/DatePicker',
	component: AppointmentSlotPicker,
}

const Template = () => {
	return <AppointmentSlotPicker />
}

export const SlotPick = Template.bind({})
