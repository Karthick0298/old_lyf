import React from 'react'
import SideNavbar from './index'

export default {
	title: 'components/SideNavbar',
	component: SideNavbar,
}

const Template = () => {
	return <SideNavbar />
}

export const SideNavbarComponent = Template.bind({})
